# Copy to .env.android.device and set YOUR_PC_IP to your computer's IPv4 address.
# Phone and PC must be on the same Wi-Fi. Get IP: Windows: ipconfig | Mac/Linux: ifconfig
REACT_APP_API_URL=http://YOUR_PC_IP:5000
