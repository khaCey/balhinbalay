# Supply values through a private environment/secret manager; never commit a populated copy.
NODE_ENV=production
DATABASE_URL=
APP_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
PROXY_KEY=
HOST=127.0.0.1
PORT=5000
